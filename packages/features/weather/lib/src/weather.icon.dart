

import 'package:flutter/material.dart';

class WeatherIcon extends StatelessWidget {
  @ovrride
  Widget build(BuildContext context) {
    return Text('yo', style: TextStyle(fontColor: Colors.red));
  }
}