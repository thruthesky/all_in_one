name: currency_converter
description: currency_converter packages
version: 0.0.1
homepage: https://github.com/thruthesky/all_in_one
repository: https://github.com/thruthesky/all_in_one

# pub.dev 에 publish 하지 않음
publish_to: "none"

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  get: ^4.3.8
  currency_picker: ^2.0.5

  wordpress:
    path: ../../services/wordpress


dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
