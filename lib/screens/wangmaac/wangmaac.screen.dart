import 'package:flutter/material.dart';

class WangmaacScreen extends StatelessWidget {
  const WangmaacScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        body: Center(
      child: Text('Wang\'s page'),
    ));
  }
}
