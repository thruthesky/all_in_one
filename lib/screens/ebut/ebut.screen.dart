class EbutScreen extends StatelessWidget {
  const EbutScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Layout(
      title: '타이틀',
      body: Text('안녕하세요. 반갑습니다.'),
    );
  }
}
