import 'package:all_in_one/widgets/layout.dart';
import 'package:flutter/material.dart';

class NakunScreen extends StatelessWidget {
  const NakunScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Layout(
      title: 'nakun',
      body: Text('nakun'),
    );
  }
}
